# STATE — текущее состояние

> Короткий файл. Читается в начале сессии. История по шагам — в `docs/ARCHIVE-PROGRESS.md` (на запрос).

## 🔜 Сейчас
**Шаг 14b-bis-1 ГОТОВ — СВОЙ BOT-ТОКЕН КЛИЕНТА: ХРАНЕНИЕ + ПОДКЛЮЧЕНИЕ** (typecheck 0, lint 0,
vitest **496/496** [+19], build ок, миграция применена локально, новый ОПЦИОНАЛЬНЫЙ env
`BOT_TOKEN_ENC_KEY`): первый из трёх подшагов «свой бот на клиента». Токен сохраняется и
проверяется; ЗАПУСК ПРОЦЕССА НЕ ТРОНУТ — поведение бота не меняется.
- РАЗБОР ДО КОДА: токен клиента меняет не «лицо», а МАРШРУТИЗАЦИЮ — `index.ts` поднимает один
  `createBot(env.BOT_TOKEN)`, и оба планировщика публикуют через этот же `bot.api`, обходя ВСЕ
  каналы глобально.
- РЕШЕНИЯ ВЛАДЕЛЬЦА: вариант A «мультибот в одном процессе» (B «процесс на клиента» с общей БД
  всё равно требует маршрутизации, с отдельной — обесценивает 14b-4/14e); разбивка на 3 подшага
  (**14b-bis-1** хранение → **14b-bis-2** мультибот в `index.ts` → **14b-bis-3** маршрутизация
  публикации/DM); токен в БД ТОЛЬКО зашифрованным; отдельная модель, а не поле на `Owner`.
- СХЕМА (миграция `..._step14bbis1_bot_account`): `BotAccount` — `ownerId @unique` (Cascade:
  отзыв владельца уносит секрет), `botUserId @unique` (🔒 один бот двум владельцам = два long
  polling = 409), `username`, `tokenCipher`, `isActive`, `lastError`.
- ЯДРА (НОВЫЕ, +19 тестов): `core/security/tokenCipher.ts` (AES-256-GCM, формат `v1:iv:tag:ct`,
  ключ через SHA-256; `decryptSecret` на любой проблеме → `null`, а не исключение — в 14b-bis-2
  битый токен не должен ронять старт) · `core/menu/botToken.ts` (`validateBotToken` до сети,
  `maskBotToken`).
- СЕРВИС `botAccountService.ts` + РЕПО `botAccountRepository.ts`: `connectBotAccount` — ключ →
  форма → «не общий бот» (`main_bot`) → `getMe` (15 с) → сверка id → `taken` → запись. Токен не
  логируется; репозиторий шифрование не знает (ключ только у сервиса).
- ЭКРАН «🤖 Мой бот» на «📡 Каналы» (у каждого владельца): инструкция BotFather → ввод токена →
  замена/отключение с подтверждением. 🔒 Сообщение с токеном бот удаляет из чата СРАЗУ, до
  проверки. ⚠️ Экран честно говорит, что публикация пока идёт через общего бота.
- ✅ ПРОВЕРЕНО на локальной БД реальным кодом (`scripts/verify14bbis1.tmp.ts`, НЕ в git, чистит
  за собой): **20/20**, `getMe` дёргался по-настоящему боевым токеном (HTTP-запрос без polling →
  409 с продом невозможен): шифротекст без токена, чужой ключ → null, `main_bot`, `taken`,
  upsert без дублей, каскад при удалении владельца.
- ⚠️ Прод: миграцию применит `prisma migrate deploy`. Чтобы экран заработал — положить
  `BOT_TOKEN_ENC_KEY` в env Railway (фраза от 16 символов, НЕ в git; потеря = ввод токенов заново).
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (в TG): «📡 Каналы» → «🤖 Мой бот»; без ключа — «не настроено»; с ключом
  прислать токен тестового бота → «подключён @…» и сообщение с токеном исчезло; токен
  @MasterPost → отказ; «🚫 Отключить» → подтверждение.
- ⏭ Дальше — **14b-bis-2**: поднять ботов клиентов в одном процессе (реестр `ownerId → Api`,
  старт/стоп N ботов, битый токен → `lastError`, процесс не падает).

**Шаг 14e ГОТОВ — ТАРИФ И СРОК ДОСТУПА ВЛАДЕЛЬЦА** (typecheck 0, lint 0, vitest **477/477**
[+13], build ок, миграция применена локально, новых env НЕТ): доступ выдавался НАВСЕГДА —
теперь у владельца есть срок, и по истечении бот закрывает меню сам. Фундамент будущей кассы.
- РЕШЕНИЯ ВЛАДЕЛЬЦА: срок фиксированный — **14 дней** всем приглашённым (не вводить руками);
  по истечении закрывается ТОЛЬКО управление, публикация в канале продолжается (планировщик
  общий с каналом супервладельца — лезть в него ради триала = лишний риск).
- СХЕМА (миграция `..._step14e_owner_plan_trial`): enum `OwnerPlan` (`trial`|`active`) +
  `Owner.plan @default(active)` + `Owner.trialUntil DateTime?`. ⚠️ Дефолт `active` НАМЕРЕННО —
  миграция не запирает тех, кто уже пользуется ботом.
- ЯДРО `core/menu/ownerPlan.ts` (НОВОЕ, +11 тестов): `checkOwnerAccess`/`checkOwnerRecordAccess`
  (правила) · `trialExpiresAt`/`extendTrialUntil` (продление действующего ДОБАВЛЯЕТ дни к
  остатку, истёкшего — считает от «сейчас») · `trialDaysLeft` · `formatOwnerPlan` (плейн-текст,
  склонение дней) · `TRIAL_DAYS=14`. 🔒 Супервладелец проходит ВСЕГДА (защита от самозапирания,
  как в `canRevokeOwner`); `trial` без даты — fail-open.
- ГЕЙТ: `resolveMenuViewer` отдаёт `MenuAccess` с ПРИЧИНОЙ (`not_invited` | `expired`) вместо
  `MenuViewer|null` — тексты для постороннего и для своего с вышедшим сроком разные. Закрыты
  все точки входа: `/menu`, callback'и, ввод текста, фото, `/start` (кнопку «📋 Меню» даём
  только с действующим доступом) и ОНБОРДИНГ (`OnboardingDeps` += `adminId`) — иначе триал
  обходился бы мимо меню через добавление бота в новый канал.
- ПРИГЛАШЕНИЕ ставит триал на 14 дней (себе — нет). ЭКРАН «👤 Владельцы»: тариф в строке
  («триал, осталось 9 дней» / «бессрочно» / «триал истёк» / «владелец бота») + кнопки
  «⏳ +14 дней» (`ownext`) и «♾ Бессрочно» (`ownact`); права — через `canRevokeOwner`.
- ✅ ПРОВЕРЕНО на локальной БД реальным кодом (`scripts/verify14e.tmp.ts`, НЕ в git, чистит за
  собой): **11/11** — срок ставится, истёкший закрывает меню с причиной `expired`, продление
  открывает снова, «Бессрочно» снимает срок, супервладелец с истёкшим триалом ВСЁ РАВНО
  проходит, миграция никого не заперла.
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (в TG): «📡 Каналы» → «👤 Владельцы» → у себя 👑 «владелец бота» без кнопок
  срока; пригласить тестовый id → «триал, осталось 14 дней»; «⏳ +14 дней» → 28; «♾ Бессрочно»
  → «бессрочно»; отзыв работает как раньше.
- ⏭ Дальше — **триал тестера** (живой прогон в TG, кода не требует) либо 14b-bis (свой
  bot-token на клиента).

**Шаг 14b-4 ГОТОВ — ЭКРАН «👤 ВЛАДЕЛЬЦЫ»: СПИСОК + ОТЗЫВ ДОСТУПА** (typecheck 0, lint 0,
vitest **464/464** [+3], build ок, миграций НЕТ, новых env НЕТ): пригласить владельца умели с
14b-1, теперь видно, у кого есть доступ, и его можно ОТОЗВАТЬ — парная операция к приглашению.
- ЯДРО `core/menu/ownerAccess.ts` (НОВОЕ, +3 теста): `canRevokeOwner` — один источник правды для
  кнопки и для перепроверки callback'а. Отзывает только супервладелец; супервладельца (себя)
  отозвать нельзя. Тексты отказов — константы `REVOKE_DENIED_NOT_ADMIN` / `_SELF`.
- РЕПО `ownerRepository`: `listOwners` (владелец + число каналов, порядок `createdAt asc` — экран
  адресует ИНДЕКСОМ) · `removeOwner` (гонка двух нажатий → `false`). 🔒 Каналы отозванного НЕ
  пропадают: `onDelete: SetNull` → бесхозные → возвращаются супервладельцу ближайшим стартом
  (`claimOrphanChannels`).
- ЭКРАНЫ: «👤 Владельцы» на «📡 Каналы» (только супервладелец) — «имя · id · N каналов», у каждого
  «🚫 Отозвать»; у супервладельца 👑 и кнопки нет. Отдельный экран подтверждения (как удаление
  поста). Гейт перепроверяется в КАЖДОМ callback'е (`own`/`ownrm`/`ownrmy`), в `ownrmy` — по
  свежему списку.
- ✅ ПРОВЕРЕНО на локальной БД реальным кодом (`scripts/verify14b4.tmp.ts`, НЕ в git, чистит за
  собой): **10/10** — счётчик, порядок, 3 правила отзыва, канал после отзыва жив и бесхозен,
  `claimOrphanChannels` вернул его супервладельцу.
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (в TG): «📡 Каналы» → «👤 Владельцы» → ты 👑 без кнопки отзыва; «🚫 Отозвать»
  → подтверждение → владелец пропал из списка и больше не открывает /menu.
- ⏭ Дальше — триал тестера (спина готова и проверена) либо 14b-bis (свой bot-token на клиента) /
  14e (тариф и поля триала на `Owner`).

**Шаг 14b-3 ГОТОВ — 14b-2 ПРОВЕРЕН НА ЛОКАЛЬНОЙ БД** (typecheck 0, lint 0, vitest **461/461**
[+0], миграций НЕТ, новых env НЕТ): проверка реальным кодом на настоящей БД (Bot API подменён
перехватчиком адресата, бот не поднимался — long polling дал бы 409 с продом). **16/16, фиксов
не потребовалось** — спина мультитенанта подтверждена, можно запускать триал тестера.
- СКРИПТ `scripts/verify14b3.tmp.ts` (НЕ в git, как `verify14b1.tmp.ts`), руками:
  `npx tsx scripts/verify14b3.tmp.ts`. Свои данные чистит сам, реальный канал не трогает.
- ЧТО ПОДТВЕРЖДЕНО: владелец канала по `channelId` (relation, не заглушка) + фолбэк на
  супервладельца · `canActOnChannel` (супервладелец над ЧУЖИМ каналом — НЕЛЬЗЯ, над бесхозным —
  можно) · адресат `sendApprovalPreview` / `notifyChannelOwner` / `ownerTargetOf` = владелец
  канала · `sendWeeklyReportNow(deps, channelId)` берёт ПЕРЕДАННЫЙ канал, а не первый в БД.
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (в TG, спина подтверждена): включить одобрение на канале тестера → пост и
  кнопки у ТЕСТЕРА; свой канал у супервладельца как раньше; «📊 Отчёт» у тестера — про его канал.
- ⏭ Дальше — триал тестера либо следующий подшаг эпика 14 по `.claude/plans/14-generic-fountain.md`.

**Шаг 14b-2 ГОТОВ — ОДОБРЕНИЕ И УВЕДОМЛЕНИЯ ПО ВЛАДЕЛЬЦУ КАНАЛА** (typecheck 0, lint 0,
vitest **461/461** [+5], build ок, миграций НЕТ, новых env НЕТ): всё «исходящее» рантайма
адресуется владельцу КАНАЛА, канал без владельца обслуживает супервладелец. Граница 14b-1 закрыта.
- ЯДРО `core/approval/access.ts` (НОВОЕ, +5 тестов): `resolveOwnerTarget` (адресат: владелец →
  фолбэк супервладелец; мусорный/нечитаемый `telegramUserId` не роняет Bot API) ·
  `canActOnChannel` (право решать судьбу поста). + репо `getOwnerTelegramIdByChannelId`.
- 🔒 ПРИНАДЛЕЖНОСТЬ ПОСТА: id очереди приходит в callback-data → проверка `pending → канал →
  владелец` в approval-композере (`ap:*`) И в меню (`api`/`apre`/`apdel`). Отказ и «не найден» —
  ОДИН нейтральный текст (чужое не раскрываем). Фильтр «только `ADMIN_ID`» в approval снят,
  режимы правки/фото ключуются по нажавшему.
- АДРЕСАТЫ: превью одобрения (`sendApprovalPreview` += `channelId`), превью «как в канале»,
  повторное превью, «нет постов», «канал не задан», AI-подхват упал, разовый пост, сигнал
  модерации комментов, «контент заканчивается», недельный отчёт — владельцу канала.
  «📊 Отчёт» → по ВЫБРАННОМУ каналу нажавшего (`sendWeeklyReportNow(deps, channelId?)`).
  ⚠️ Сигнал «MTProto не настроен» намеренно остался супервладельцу (чинится в env Railway).
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (в TG): включить одобрение на канале тестера → пост и кнопки приходят ТЕСТЕРУ;
  свой канал у супервладельца работает как раньше; «📊 Отчёт» у тестера — про его канал.
- ⏭ Дальше — триал тестера (спина готова) либо следующий подшаг эпика 14 по
  `.claude/plans/14-generic-fountain.md`.

**Шаг 14b-1 ГОТОВ — СПИНА МУЛЬТИТЕНАНТА** (typecheck 0, lint 0, vitest **456/456** [+4],
build ок, миграций НЕТ, новых env НЕТ): меню открыто любому зарегистрированному владельцу
(`Owner`), каждый видит ТОЛЬКО свои каналы. После этого шага можно запускать триал тестера.
- ГЕЙТ: `resolveMenuViewer` (`findOwnerByTelegramId`) вместо `ctx.from.id === adminId` — `/menu`,
  callback'и, ввод. Типы: `AdminBotDeps` (старт) vs `AdminDeps` += `viewer {userId, ownerId}`
  (собирается на каждый апдейт) — 82 вызова резолверов экранов не тронуты. Эфемерные состояния
  (`pending`/`drafts`/выбранный канал) ключуются по `viewer.userId`.
- СКОУП: `resolveChannelMenu` → `listChannelsByOwner(viewer.ownerId)`. 🔒 Дыра переключателя
  закрыта структурно: канал берётся по индексу в списке ВЛАДЕЛЬЦА + `pickSelectedId` отбрасывает
  id вне списка — крафтнутый callback до чужого канала не дотягивается.
- ЛОВУШКА 14a ЗАКРЫТА: `createChannel` += обязательный `ownerId` («➕ Добавить канал» штампует).
- ОНБОРДИНГ: промоут от любого зарегистрированного владельца; `registerChannelFromOnboarding`
  += `ownerId` (у существующего канала — только при `NULL`, чужое НЕ перехватывается); DM
  «подключён» → подключившему, «разжаловали/убрали» → владельцу КАНАЛА
  (`getOwnerTelegramIdByChatId`) — теперь и когда права снял другой админ.
- ПРИГЛАШЕНИЕ: «➕ Пригласить владельца» на «📡 Каналы» (только супервладелец, `invown` с
  перепроверкой) → ввод `<tg user id> [имя]` (`validateOwnerInvite`, +4 теста) → `ensureOwner`.
  `/start` даёт кнопку «📋 Меню» любому зарегистрированному владельцу.
- ⚠️ ГРАНИЦА (ЗАКРЫТА в 14b-2): превью одобрения и результат AI-поста по-прежнему у СУПЕРВЛАДЕЛЬЦА
  (approval-композер слушает только его). Тестеру одобрение не нужно (дефолт ВЫКЛ → прямая публикация).
- ✅ ПРОВЕРЕНО на локальной БД (реальный код, без запуска бота): 12/12 — гейт (супервладелец
  проходит, левый id нет), приглашение, скоуп (тестер видит только своё), штамп владельца при
  создании/онбординге, чужой канал НЕ перехватывается, владелец канала находится по chatId.
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (проверка в TG): /menu работает как раньше; «📡 Каналы» → видна кнопка
  «➕ Пригласить владельца»; для триала: пригласить тестера по id → тестер /menu → добавляет
  бота админом своего канала → канал у него в «📡 Каналы», чужого не видно.
- ⏭ Дальше — **14b-2**: разграничение одобрения постов/кнопок по владельцу канала + служебные
  уведомления владельцу канала (превью одобрения → владельцу, approval-композер пускает владельцев).

**Шаг 14a ГОТОВ — СТАРТ ЭПИКА 14** (typecheck 0, lint 0, vitest **452/452**, build ок,
миграция применена локально, новых env НЕТ):
ФУНДАМЕНТ МУЛЬТИТЕНАНТА — модель `Owner` + `Channel.ownerId`. Видимой фичи нет, поведение бота
НЕ меняется. План эпика: `.claude/plans/14-generic-fountain.md`.
- ЗАЧЕМ: владелец хочет дать ОДНОМУ тестеру бесплатный доступ на его канале. Разбор показал: Шаг 14
  роадмапа (веб-дашборд + биллинг + API) для этого НЕ нужен — касса бессмысленна без платящих, сайт
  избыточен (продукт целиком живёт в Telegram). Блокер — мультитенант (хвост Шага 9): бот
  одно-арендный, у канала нет владельца, меню закрыто одним `ADMIN_ID`, онбординг игнорирует чужих.
- РЕШЕНИЯ ВЛАДЕЛЬЦА: полный Шаг 14 как эпик, но в порядке «сначала триал, потом касса»; триал —
  ТОЛЬКО через Telegram, БЕЗ сайта (14c–14d отложены); тестер на общем боте (свой bot-token = 14b-bis).
- СХЕМА (миграция `..._step14a_owner_channel_ownerid`): `Owner` (`telegramUserId` @unique строкой,
  опц. `name`; тариф/триал — на 14e) + `Channel.ownerId String?` (`onDelete: SetNull`) + индекс.
  ⚠️ Nullable НАМЕРЕННО: по `ownerId` пока НИЧЕГО не фильтруется → канал без владельца как раньше.
- РЕПОЗИТОРИЙ `ownerRepository.ts` (НОВЫЙ): `ensureOwner` (идемпотентный upsert) ·
  `findOwnerByTelegramId` (основа гейта 14b) · `claimOrphanChannels` (бэкофилл `ownerId: null` →
  супервладельцу). + `channelRepository.listChannelsByOwner` (пока не используется — для 14b).
- ВЖИВЛЕНИЕ `index.ts`: старт → `ensureOwner(ADMIN_ID)` → `claimOrphanChannels`. Самоисцеление
  (калька `ensureCampaignStart` 11a), а не разовый скрипт: прод поднимается `npm start` без ручных
  шагов. Сбой не фатален (лог) — на 14a от владельца ничего не зависит.
- ✅ ПРОВЕРЕНО на локальной БД (реальный код, без запуска бота — long polling на боевом токене дал бы
  409 с продом): сирот ДО=1 → присвоен 1; повтор → тот же `ownerId`, присвоено 0; сирот 0;
  `findOwnerByTelegramId(7035079048)` → владелец; `listChannelsByOwner` → «Таро · София».
- ⚠️ Прод: миграцию применит `prisma migrate deploy` (в `npm start`), бэкофилл сделает сам старт.
- ⏭ Дальше — **14b**: гейт «зарегистрированный владелец» вместо `ADMIN_ID` (`admin/index.ts:190`,
  `approval`, `postButtons`) · скоуп переключателя на `listChannelsByOwner` (`channelContext.ts`) ·
  онбординг принимает промоут от любого владельца + штампует `ownerId` + DM владельцу КАНАЛА ·
  экран «➕ Пригласить владельца». ⚠️ Ловушка: `createChannel` (8a) владельца не штампует.

**ПОНЯТНОСТЬ ЦИФР ГОТОВА — 3 подшага** (по жалобам владельца 2026-07-15; typecheck 0, lint 0,
vitest **452/452** [+23], build ок, миграций НЕТ, новых env НЕТ, старые callback-коды не тронуты):
бот показывал число/превью, но не показывал, что за ними стоит. Все три жалобы подтвердились кодом.
- **1. ССЫЛКА НА ПОСТ В ОТЧЁТЕ** («пишет пост с луной, а где ссылка»): новое ядро
  `core/analytics/postLink.ts` (`buildPostLink`) — превью поста и «🏆 Лучший пост» стали
  Markdown-ссылками `t.me/<username>/<id>` (приватный канал → `t.me/c/…`). Без username/chatId —
  мягкий откат на курсив. Миграций нет: `messageId` уже был в метрике. ⚠️ Курсива внутри ссылки нет
  (legacy Markdown не умеет вложенные сущности), `cleanPreview` вырезает `[]()` — иначе скобка в
  тексте поста порвала бы разметку и отчёт целиком ушёл бы плейн-текстом. Пустое превью → «без подписи».
- **2. РАСШИФРОВКА «ОДОБРЕНИЕ (13)»** («что значит 13 и где эти 13»): экрана со списком очереди
  НЕ СУЩЕСТВОВАЛО, до поста можно было добраться только через превью в личке. Новое ядро
  `core/approval/queueSummary.ts` → «13 постов ждут решения · 9 из плана, 4 от AI · самый старый
  от 03.07» (источник по `externalId`: null = AI) + список с пагинацией + карточка поста
  (`api`/`apre`/`apdel`). ⚠️ `approvalKeyboard` на экран меню НЕ вешали: её обработчики
  (`editResolved`) переписывают сообщение с кнопкой — на меню это стёрло бы навигацию. Карточка шлёт
  превью отдельным сообщением (`resendApprovalPreview`). TTL очереди — вне скоупа (чистим руками).
- **3. НЕДЕЛИ ПЛАНА** («крутится 1 неделя, не переходит на 2-ю») — 🐞 НАСТОЯЩАЯ ПРИЧИНА:
  `upsertChannel` делал `update: data`, а сид гнал `campaignStart: null` → **каждый `npm run seed`
  затирал старт кампании**, после чего `resolveCampaignDay` навсегда отдавал «неделю 1».
  Фикс: `campaignStart` убран из `ChannelSeed`/`upsertChannel` — старт пишет ТОЛЬКО
  `ensureCampaignStart` (регресс теперь невозможен структурно). Новый `npm run backfill-campaign-start`
  чинит затёртый старт по дате первой публикации; если публикаций не было — старт не выдумывает.
  Экраны: рядом с неделей «план идёт с 08.07» / «план ещё не начат», причина остановки — на всех
  трёх экранах. Новых кнопок нет (просьба владельца).
- ℹ️ Ответ на вопрос владельца: **AI НЕ делает посты на неделю вперёд.** Пакетной генерации в коде
  нет; AI пишет один пост в момент слота, если план на слот пуст и включён «🤖 AI-подхват».
  Максимум в очередь = число слотов `autopost_times` в день (дефолт 2 — 10:00 и 20:00).
- ⏳ ЗА ВЛАДЕЛЬЦЕМ (проверка в TG): (а) «📈 Рост → 📨 Отчёт по просмотрам → прислать сейчас» —
  кликнуть ссылку, убедиться что открывается тот пост и отчёт пришёл размеченным (плейн-текст =
  сломанный Markdown); (б) «📋 Одобрение» — список 13 постов, разбивка сходится, «❌ Убрать из
  очереди» уменьшает счётчик; (в) решить, запускать ли `npm run backfill-campaign-start` на Railway.

**Шаг 12g ГОТОВ — ЭПИК 12 ЗАКРЫТ** (typecheck 0, lint 0, vitest **429/429** [+22], build ок,
миграций НЕТ, новых env НЕТ):
ВЕТ ЧУЖИХ КАНАЛОВ перед закупкой рекламы — экран «🔍 Проверка канала». Владелец вводит
`@username` → бот тянет Telemetr → автоматический вердикт первого прохода 🟢/🟡/🔴. Порт чек-листа
скилла `telemetr-vetting` в объёме HTTP-API. План `.claude/plans/12g-channel-vetting.md`.
- API отдаёт больше, чем читала секция «Рынок»: `mentioning_channels_count` (в скольких РАЗНЫХ
  каналах упоминали — ДЕТЕКТОР СЕТКИ: органика ~20 vs зодиак-ферма 725) + `scoring_rate` (оценка
  0..10). Калибровка на 3 якорях скилла (проверено live через MCP telemetr).
- ЯДРО `core/market/channelVetting.ts` (ЧИСТОЕ, +22 теста): `vetChannelStat` (6 сигналов —
  охват/подписчик, сеть упоминаний, ER, оценка, тренд 28д, аномалии 12f) + `buildVettingReport`
  (плейн-текст, без Markdown — правило 12c). Пороги = именованные константы (niche-agnostic).
  Правило вердикта = порт рубрики скилла «🔴 ≥2 флага», НО сетка/накрутка (Шаги 4/5, «главные») —
  РЕШАЮЩИЕ: любой из них 🔴 тянет вердикт в 🔴 сам. Тип `ChannelVettingStat` (marketData.ts).
- АДАПТЕР `telemetrProvider.ts`: метод `fetchChannelVetting` (расширенная zod-схема стата) в
  интерфейс `MarketDataProvider`. Ряд подписчиков переиспользует `fetchSubscriberHistory` (12e-2).
- СЕРВИС `channelVettingService.ts`: `normalizeChannelRef` (@username / t.me/x / telemetr.me/@x →
  `@username`; приватная числовая ссылка → null) + `vetChannel` (union `no_key|bad_ref|not_found|ok`).
  БЕЗ кэша: чужой канал не имеет строки `Channel`, вет по требованию (2 запроса Telemetr/проверку).
- МЕНЮ: кнопка «🔍 Проверка канала» на «📈 Рост» (`vet`) → prompt → pending `vetChannel` (обработан
  ДО проверки своего канала, как `addChannel`) → ответ вердиктом + возврат на «Рост».
- ⚠️ Прод: работает при `TELEMETR_API_KEY` в env Railway (локально ключа нет → `no_key`, проверено).
  НЕ замена ручного вета: API не видит имена зодиак-клонов, историю описаний, тон/этику — экран это
  честно проговаривает (доразбор t.me/s + telemetr.me). Пер-канально не нужно (глобальный инструмент).
- ⏭ Дальше — эпик 12 закрыт (12a–12g). Кандидаты: 12e-3 (похожие каналы + бенчмарк ниши, когда
  эндпоинт Telemetr станет надёжнее) / SaaS-онбординг / следующий шаг роадмапа (Шаг 14 — отдельная фаза).
- ⏳ За владельцем — проверка в TG: «📈 Рост» → «🔍 Проверка канала» → @favoritehoro (ждём 🟢) и
  @ezoterik_dnevnik (ждём 🔴).

**РЕДИЗАЙН МЕНЮ (вар. B) — ГОТОВ, подшаги 1–3** (typecheck 0, lint 0, vitest **407/407**, build ок,
миграций НЕТ, env НЕТ, callback-коды не тронуты): чистый UI/навигация, 0 бизнес-логики.
Плоское меню из 13 кнопок → быстрые действия сверху + 4 раздела. Проект/диагностика — в `.claude/plans/`
(варианты A/B + карта «старое→новое»); интерактивный макет вар. B — Artifact. Решение владельца
(2026-07-13): вариант B, 3 подшага. Все 13 старых функций достижимы (сверено по карте).
- ГЛАВНЫЙ ЭКРАН (`renderMain`): быстрые действия «➕ Новый пост / 🤖 AI-пост» + «Одобрение» с
  ДИНАМИЧЕСКИМ бейджем (`countPending`: «✅ Одобрение (N)» при N>0, иначе «📋 Одобрение»); ниже —
  4 раздела «📅 План · 💬 Комментарии · 📈 Рост · 📡 Каналы» + slim «⚙️ Настройки».
- 📅 ПЛАН (`cal`=renderCalendar): слиты «Календарь»+«Контент-план» (вход = текущая неделя, внутри
  «🗂 Весь план»=`plan`) + перенесены «📆 Автопостинг»(`auto`) и «🔘 Кнопки постов»(`bpl`).
- 💬 КОММЕНТАРИИ (`set`=renderSettings, бывш. «Настройки»): мастер-тумблер + «💬 Триггеры»(`trg`,
  перенос) + «🤖 AI-ответы» + «🛡 Модерация» + «⏱ Кулдаун» + «📋 Сводка»(`stat`, бывш. «Статус»).
- 📈 РОСТ (`grow`=renderGrowth): инсайты + «🧪 Эксперименты»(`exp`) + «📨 Отчёт по просмотрам»
  (`an`=renderAnalytics, бывш. «Аналитика») + тумблер AI-пересказа.
- ⚙️ НАСТРОЙКИ slim (НОВЫЙ `gset`=renderGlobalSettings): канал/пояс + вход в «📡 Каналы»; про
  комментарии тут ничего (ушло в «💬 Комментарии») — прямой ответ на исходную жалобу владельца.
- НАВИГАЦИЯ «◀ Назад» ровная: plan/auto/bpl→cal; trg/stat→set; an→grow; gset→home.
- ⏳ За пользователем — проверка в TG: /menu → пройти все 4 раздела + быстрые действия, «◀/🏠».

**Шаг 13f ГОТОВ — ЭПИК 13 ЗАКРЫТ** (typecheck 0, lint 0, vitest **407/407** [+11], build ок,
миграций НЕТ, новых env НЕТ):
AI-СОВЕТНИК ЭКСПЕРИМЕНТОВ — опциональный «что тестировать следующим». Haiku по инсайтам 12c
(`buildGrowthReport`) предлагает ОДНО измерение каталога 13a с обоснованием; владелец жмёт
«✅ Запустить» → эксперимент стартует существующим путём `xstart`/`startExperiment`. Переиспользует
`ANTHROPIC_API_KEY` + общий `ai_daily_cap`. Решения владельца (2026-07-13): где — экран
«🧪 Эксперименты» (расход по требованию); подтверждение — сразу запуск; тумблер отдельный, дефолт ВЫКЛ.
- КАТАЛОГ `experiment.ts`: `EXPERIMENT_DIMENSION_KEYS` (кортеж `as const`) — единый источник
  типа `ExperimentDimension` и enum-валидации ответа модели.
- ЯДРО `core/experiments/buildAdvisorPrompt.ts` (ЧИСТОЕ, +8): `buildAdvisorPrompt` (тон+инсайты+
  каталог; опц. `settledLabels` = непросроченная стратегия 13e → «предпочти другое»);
  `ADVISOR_JSON_SCHEMA` (Structured Outputs, `dimension` ⊂ enum каталога); `parseAdvisorVerdict`
  (zod, вне каталога/пусто/перебор/кривой JSON → null; чистит Markdown — правило 12c).
- НАСТРОЙКА `experimentAdvisorSettings.ts`: тумблер `experiment_advisor_enabled` (дефолт ВЫКЛ,
  калька 12d). СЕРВИС `experimentAdvisorService.ts` (+3): `generateAdvice` (Haiku, мягкая
  деградация, клиент инъектируется) + `adviseNextExperiment` (ворота тумблер→ключ→канал→бюджет
  ДО вызова→`getLearnedStrategy`→Haiku→подпись каталога; отказ → null, расход 0).
- ЭКРАН `renderExperiments`: тумблер «🔮 AI-советник» (`xadvtgl`) + кнопка «🔮 Совет: что
  тестировать?» (`xadvise`, при ВКЛ). Новый `renderExperimentAdvice` (совет + «✅ Запустить»
  через существующий `xstart`). Отчёт ПН НЕ трогали (решение владельца — только экран).
- ⚠️ Прод: совет виден при `ANTHROPIC_API_KEY` + ВКЛ тумблер; каждое нажатие «Совет» = 1 вызов
  Haiku (единица общего `ai_daily_cap`). Пер-канально (SaaS).
- ⏭ Дальше — эпик 13 ЗАКРЫТ (13a–13f). Кандидаты: 12g (вет чужих каналов) / следующий шаг роадмапа.

**Шаг 13e ГОТОВ** (typecheck 0, lint 0, vitest **396/396** [+18], build ок, миграций НЕТ):
ПЕТЛЯ САМООПТИМИЗАЦИИ (Optimization Engine) — замыкает эпик 13. Победитель эксперимента
записывается в «выученную стратегию» канала (JSON в `Setting`, без миграции) и подмешивается
в промпт генерации следующих AI-постов. План `.claude/plans/13-experiments.md`.
- ЯДРО `core/experiments/learnedStrategy.ts` (ЧИСТОЕ, +15 тестов): `LearnedStrategyEntry`,
  `parseLearnedStrategy` (zod), СРОК ГОДНОСТИ `STRATEGY_TTL_WEEKS=6` (`isStrategyExpired`/
  `strategyDaysLeft`), `recordWinner` (одна запись на измерение), КВОТА `EXPLORATION_ONE_IN=4`
  (`isExplorationPost` — каждый 4-й пост разведочный, 25%), `buildStrategyDirective` (активные
  записи кроме измерения под экспериментом → директивы каталога), `buildStrategySummary`
  (плейн-текст, БЕЗ Markdown-эмфазы — правило 12c).
- СЕРВИС `services/experiments/optimizationService.ts` (+3 теста): `Setting`-ключи
  `learned_strategy` / `strategy_apply_counter` / `strategy_auto_apply` (дефолт ВЫКЛ).
  `resolveAiGeneration` → `{variantDirective, variantKey}`: вариант эксперимента (13c) +
  стратегия, если пост не разведочный. `applyExperimentWinner`: вердикт `winner` → запись в
  стратегию + `stopActiveExperiment`; `suspicious` (guard) НЕ применяем; мало данных →
  `not_ready`. `maybeAutoApplyExperimentWinner` — гейт по тумблеру для джоба.
- РЕФАКТОР `experimentService`: `computeExperimentVerdict` + `formatExperimentProgress`;
  `buildExperimentProgress` = их композиция (тот же вердикт нужен экрану/отчёту/применению).
- ГЕНЕРАЦИЯ `aiPostApprovalService.buildAiDraft`: `assignExperimentVariant` → `resolveAiGeneration`
  (директива = эксперимент + стратегия). Прямая публикация без одобрения — нейтральна.
- ЭКРАН `renderExperiments`: блок «📚 Выученная стратегия» + тумблер «🔁 Авто-применение» (`xauto`);
  при `winner` — кнопка «✅ Применить победителя» (`xapply`). ОТЧЁТ (`runWeeklyReport`, ПН):
  после отправки — `maybeAutoApplyExperimentWinner`, при `applied` уведомление владельцу.
- ⚠️ Прод: миграций/env нет. Авто-применение дефолт ВЫКЛ (владелец применяет кнопкой);
  стратегия наполняется по мере вердиктов. Пер-канально (SaaS).
- ⏭ Дальше — **13f** (опц.): AI-советник «что тестировать» (Haiku по инсайтам 12c, кнопка
  подтверждения, бюджет `ai_daily_cap`). Либо 12g (вет чужих каналов) / следующий шаг роадмапа.

**Шаг 13d ГОТОВ** (typecheck 0, lint 0, vitest **378/378** [+5], build ок, миграций НЕТ):
ЭКРАН «🧪 Эксперименты» + СЕКЦИЯ В ОТЧЁТЕ + ПРИВЯЗКА `variantKey` к `PostMetric` — первая
ВИДИМАЯ фича эпика 13. Владелец запускает/останавливает A/B и видит прогресс. Колонки
`PostMetric.variantKey/origin` уже были с 13b → миграции нет. План `.claude/plans/13-experiments.md`.
- ФОРМАТТЕР `core/experiments/experimentReport.ts` (ЧИСТЫЙ, +6 тестов): `buildExperimentReport`
  → «строка на вариант (N постов + ERR) · статус». Статусы 13a: continue/no_difference/winner/
  suspicious. ⚠️ БЕЗ Markdown-эмфазы (правило 12c) — один текст и на экран, и в отчёт.
- ПРИВЯЗКА ВАРИАНТА (иначе вердикт не сгруппировать): ПРЕД-ЗАСЕВ снимка при публикации.
  `sendPost` возвращает `message_id` (было void); `publishPending` при `variantKey != null` →
  `postMetricRepository.seedVariantMetric` (upsert по `[channelId,messageId]`, `origin=ai`+
  `variantKey`, метрики 0). Сбор MTProto (`upsertPostMetric`) в update эти поля НЕ трогает →
  вариант доживает до вердикта. `message_id` Bot API == id в MTProto. Best-effort (сбой пометки
  не «отменяет» пост). Вариант живёт ТОЛЬКО на пути одобрения (прямая публикация — не в эксп.).
- ЧТЕНИЕ: `listVariantMetricsSince` (снимки с `variantKey`, совместимы с `EngagementLike`) ·
  `getSubscriberDeltaSince` (guard-метрика: Δ подписчиков от первого снимка к последнему; <2 → null).
- СЕРВИС `experimentService.buildExperimentProgress(prisma, channelId, timezone)`: активный эксп.
  → метрики по вариантам → `evaluateExperiment(subscriberDelta)` → `buildExperimentReport`. Нет
  активного / измерение выпало из каталога → null. `startedLabel` = дата старта в поясе «ДД.ММ».
- ЭКРАН `renderExperiments` (кнопка «🧪 Эксперименты» на «📈 Рост»): идёт → прогресс + «⏹ Остановить»;
  не идёт → 4 кнопки запуска по измерениям каталога. Роутер `exp`/`xstart`(индекс)/`xstop`.
- ОТЧЁТ (`weeklyReportService`): секция эксперимента после AI-пересказа роста, перед «🌍 Рынок».
  Нет активного → блока нет. 0 токенов, из БД.
- ⚠️ Прод: миграций/env нет. Экран доступен сразу; прогресс наполняется по мере сбора метрик
  (снимок 22:00 МСК / отчёт ПН) — без MTProto выборки пустые. Тесты `sendPost` обновлены под
  новый возврат `message_id` (моки API отдают `{message_id}`).
- ⏭ Дальше — **13e**: петля самооптимизации — выученная стратегия (победитель на измерение +
  срок годности) в `Setting`, кнопка «применить победителя», тумблер авто-применения (дефолт
  ВЫКЛ), подмешивание в `buildPostPrompt`, квота исследования ~75/25. Опц. 13f (AI-советник).

**Шаг 13c ГОТОВ** (typecheck 0, lint 0, vitest **373/373** [+4], build ок, миграций НЕТ):
ВЖИВЛЕНИЕ ЭКСПЕРИМЕНТА В ГЕНЕРАЦИЮ — вариант активного эксперимента теперь реально влияет
на AI-пост (директива формы в промпт) + якорь голоса в образцах. План `.claude/plans/13-experiments.md`.
- КАТАЛОГ `core/experiments/experiment.ts`: `ExperimentVariant` += `directive` — текст-указание
  по ФОРМЕ поста (CTA/фото/длина/заголовок), niche-agnostic (тему по-прежнему диктуют образцы).
  Прописаны все 8 вариантов (4×2).
- ПРОМПТ `core/ai/buildPostPrompt.ts`: `PostPromptInput` += опц. `variantDirective`; непустая →
  блок «Особое указание для этого поста: …» в user после темы; пусто/null → блока нет.
- СЕРВИС `experimentService.ts`: `assignExperimentVariant` → `ExperimentAssignment {variantKey,
  directive}` (было — только ключ): генерация и хранение опираются на ОДИН индекс ротации.
- СБОРКА `aiPostApprovalService.buildAiDraft`: назначение варианта переехало ВНУТРЬ — после
  образцов (дешёвые отказы индекс не тратят), но ДО генерации (директива → промпт, ключ →
  `draft.variantKey`). Флаг `participateInExperiment` (дефолт true): кнопка AI-пост → true;
  автоподхват `placeAiFallbackPost` → `=approvalOn` (прямая публикация без одобрения не участвует,
  как в 13b). Компромисс vs 13b: редкая неудача генерации «съедает» индекс ротации — паритет
  самовосстанавливается, вердикт (13d) считает реальные метрики.
- ЯКОРЬ ГОЛОСА `postRepository.getSamplePosts`: `orderBy [origin asc, externalId asc]` — enum
  `PostOrigin` объявлен human→ai, Postgres сортирует по объявлению → человеческие посты первыми.
- ⚠️ Прод: поведение не меняется, пока эксперимент негде запустить (13d) — без активной строки
  `Experiment` директивы нет, генерация как раньше. Новых env/миграций нет.
- ⏭ Дальше — **13d**: экран «🧪 Эксперименты» (запуск/остановка + прогресс по вариантам) +
  секция активных экспериментов/вердиктов в еженедельном отчёте; привязка `variantKey` к
  `PostMetric` при сборе метрик (чистые выборки для вердикта 13a). Затем 13e (петля
  самооптимизации: выученная стратегия в `Setting`, квота ~75/25, срок годности победителя).

**Шаг 13b ГОТОВ** (typecheck 0, lint 0, vitest **369/369** [+5], build ок, миграция применена локально):
ХРАНЕНИЕ И ПРИВЯЗКА Experiment Engine — БД-скелет поверх ядра 13a. Видимой фичи нет (эксперименты
негде запустить — экран/отчёт в 13d), но AI-пост при активном эксперименте уже получает вариант
(ротация) при постановке в очередь. План: `.claude/plans/13-experiments.md`.
- МИГРАЦИЯ `..._step13b_experiments_variant_origin`: enum `PostOrigin`(human|ai) + `ExperimentStatus`
  (active|stopped); таблица `Experiment` (channel, dimension, status, `assignedCount`-счётчик ротации,
  startedAt/stoppedAt); `PendingPost`+=`variantKey?`, `PostMetric`+=`origin`/`variantKey?`,
  `Post`+=`origin` (дефолт `human` — посты плана написаны людьми, якорь голоса для 13c).
- РЕПОЗИТОРИЙ `experimentRepository.ts`: `getActiveExperiment` · `startExperiment` (инвариант «один
  активный на канал»: стопит предыдущий) · `stopActiveExperiment` · `takeNextVariantIndex` (атомарный
  `increment` — резервирует индекс ротации без гонки).
- СЕРВИС `experimentService.ts` `assignExperimentVariant`: активный эксп. → спец. измерения (13a) →
  атомарный индекс → `assignVariant` → ключ. Нет эксп./неизвестное измерение → null (счётчик не двигаем);
  ошибка глотается → null (не роняет публикацию).
- ВЖИВЛЕНИЕ: `ApprovalDraft`+=опц.`variantKey` (только AI-путь; плановый пост → null), проброшен в
  `createPending`. Вариант назначается ПОСЛЕ успешной сборки черновика в 2 точках очереди: кнопка
  «🤖 AI-пост» + автоподхват при одобрении ВКЛ. Прямая публикация (одобрение ВЫКЛ) вариант пока НЕ
  пишет — привязка к `PostMetric` в 13c/13d.
- ⚠️ Прод: миграцию на Railway применит `prisma migrate deploy`; поведение бота не меняется, пока нет
  способа запустить эксперимент (13d) — без строки `Experiment` `assignExperimentVariant` = null.
- ⏭ Дальше — **13c**: `buildPostPrompt` += опц. `variantDirective` (текст-директива варианта из
  каталога 13a); AI-пост (10b) и автоподхват (10c) при активном эксперименте получают директиву;
  `getSamplePosts` — приоритет `origin=human`. Затем 13d (экран «🧪 Эксперименты» + секция в отчёте) →
  13e (петля самооптимизации: выученная стратегия в `Setting`, квота ~75/25, срок годности победителя).

**Шаг 13a ГОТОВ** (typecheck 0, lint 0, vitest **364/364** [+13], build ок, миграций НЕТ):
ЯДРО Experiment Engine — старт эпика 13 «Optimization + Experiment Engine» (модули 6+8
из План.txt). План эпика с разбивкой 13a–13f: `.claude/plans/13-experiments.md`.
0 AI, ни Telegram, ни БД — только `core/experiments/` + тесты (как 12a). Видимой фичи нет.
- РЕШЕНИЯ ВЛАДЕЛЬЦА (зафиксированы в плане): эксперименты ТОЛЬКО над AI-постами (вариант =
  директива промпта, пулы не трогаем); 4 фиксированных измерения (CTA/фото/длина/заголовок);
  применение победителя — кнопкой, тумблер авто-применения дефолт ВЫКЛ; 12g остаётся в очереди.
- ОГРАНИЧЕНИЕ: настоящего A/B в TG-канале нет → эксперимент ПОСЛЕДОВАТЕЛЬНЫЙ (ротация
  вариантов между постами), одно измерение за раз, ~2–4 недели на вердикт.
- МОДУЛИ: `experiment.ts` (каталог `EXPERIMENT_DIMENSIONS` 4×2, niche-agnostic) ·
  `assignVariant.ts` (детерминированная ротация по счётчику) · `evaluateExperiment.ts`
  (вердикт `continue|no_difference|winner|suspicious`; анти-слоп: фильтр выбросов 12a по
  общему пулу просмотров + guard-метрика — победитель при оттоке подписчиков → `suspicious`;
  `MIN_POSTS_PER_VARIANT=5`, `WINNER_THRESHOLD_PCT=15`).
- BACKLOG += SaaS-онбординг (профиль голоса; сид не тиражировать) + 13f (AI-советник).
- ⏭ Дальше — **13b**: миграция `Experiment` + `variantKey` (`PendingPost`/`PostMetric`) +
  `origin` human|ai (якорь голоса — `getSamplePosts` предпочтёт человеческие посты),
  репозиторий, назначение варианта при постановке AI-поста в очередь. Затем 13c
  (директивы в `buildPostPrompt`) → 13d (экран/отчёт) → 13e (петля самооптимизации:
  выученная стратегия в `Setting`, квота исследования ~75/25, срок годности победителя).

**Шаг 12f ГОТОВ** (typecheck 0, lint 0, vitest **351/351** [+10], build ок, миграций НЕТ):
ФИЛЬТРЫ КАЧЕСТВА ДАННЫХ (риск №5 «не учиться на мусоре»). 0 AI, 0 новых запросов к Telemetr,
новых env НЕТ. План: `.claude/plans/12f-data-quality.md`.
- ЧАСТЬ 1 — тренд БЕЗ выбросов (фикс 12a): `insights.ts` теперь считает `flagViewOutliers`
  для ОБОИХ окон и фильтрует выбросы перед `compareTrend` — рекламный/виральный пост больше
  не даёт ложный «рост охвата» (и ложное «падение», попав в прошлое окно). `Insights.outliers`
  не тронут; окно <4 постов → как раньше. Потребители тренда читают только направление/Δ%.
- ЧАСТЬ 2 — детект накрутки (НОВОЕ ядро `core/market/subscriberAnomaly.ts`):
  `detectSubscriberAnomalies` по ряду подписчиков 12e-2 из кэша — суточный скачок
  |Δ| ≥ max(10 чел, 5% базы) → `{date, delta}`; дырки в ряду не считаются скачком.
  Вывод: строка `⚠️ Резкие скачки подписчиков: N за 28д (ДД.ММ…) — возможна накрутка или
  рекламный всплеск` в секции «🌍 Рынок» (`marketSection.ts` += параметр `anomalies`;
  `buildMarketSectionText` считает их сам) — автоматом на экране «Рост» и в отчёте ПН.
- ⏭ Дальше — **12g**: вет ЧУЖИХ каналов перед закупкой рекламы (экран «🔍 Проверка канала»;
  адаптер уже умеет любой публичный канал, ядро аномалий переиспользуется, порт чек-листа
  из скилла telemetr-vetting); 12e-3 (похожие каналы + бенчмарк) — когда эндпоинт Telemetr
  станет надёжнее.

**Шаг 12e-2 ГОТОВ** (typecheck 0, lint 0, vitest **341/341** [+10], build ок, миграций НЕТ):
РЫНОЧНЫЕ ДАННЫЕ (Telemetr), вторая половина шва — динамика подписчиков + секция «🌍 Рынок»
в еженедельном отчёте. Скоуп согласован с владельцем; похожие каналы и бенчмарк ниши ОТЛОЖЕНЫ
(эндпоинт `/v1/channels/similar` при разведке дважды упал по таймауту + риск 429). 0 AI-вызовов.
План: `.claude/plans/12e2-market-telemetr.md`.
- РАЗВЕДКА API: `/channels/subscribers?group=day` → `[{date, participantsCount}]` (новые
  сверху); ⚠️ окно ровно в месяц → HTTP 400, 28 дней — ок; даты `YYYY-MM-DD HH:MM:SS`.
- ЯДРО (`core/market/`): `subscriberDynamics.ts` (НОВЫЙ — `computeSubscriberDynamics` →
  `{current, delta7d, delta28d}`, база Δ = последняя точка не моложе границы окна, истории
  меньше окна → null) · `marketData.ts` (`SubscriberPoint`, провайдер += `fetchSubscriberHistory`)
  · `marketCache.ts` (кэш += опц. `subscribers`, формат 12e-1 валиден) · `marketSection.ts`
  (строка `📈 Подписчики за 7д: +3 · за 28д: +14`, знак всегда).
- АДАПТЕР: общий `requestJson` + `fetchSubscriberHistory` (окно 28 дней, те же правила
  деградации → null + warn). СЕРВИС: `getMarketData` (стата + ряд, 2 запроса раз в 12ч;
  упал только ряд → ряд из старого кэша).
- ОТЧЁТ: `WeeklyReportDeps` += `telemetrApiKey?`; `collectReport` принимает канал целиком
  (`ReportChannel`, нужен username) и добавляет секцию ПОСЛЕ нарратива (в Haiku рынок не
  скармливаем). Прокинуто в планировщик и тест-кнопку `anrep`.
- ⚠️ Прод: новых env НЕТ (ключ положен в 12e-1). Строка динамики появится после протухания
  кэша (TTL 12ч); секция в отчёте — в ближайший ПН 09:30 МСК или по тест-кнопке.
- ⏭ Дальше (решение владельца) — **12f**: фильтры качества данных (план
  `.claude/plans/12f-data-quality.md`): (1) тренд 12a БЕЗ выбросов — сейчас
  `compareTrend` в `insights.ts` получает окна целиком и рекламный пост даёт ложный
  «рост охвата»; (2) детект резких скачков подписчиков по ряду 12e-2 (накрутка/
  всплеск) → строка-предупреждение в секции «🌍 Рынок». Затем **12g** — вет чужих
  каналов перед закупкой рекламы (адаптер уже умеет любой публичный канал);
  12e-3 (похожие каналы + бенчмарк) — когда эндпоинт Telemetr станет надёжнее.

**Шаг 12e-1 ГОТОВ** (typecheck 0, lint 0, vitest **331/331** [+11], build ок, миграций НЕТ):
РЫНОЧНЫЕ ДАННЫЕ (Telemetr) за адаптером `MarketDataProvider` — взгляд СНАРУЖИ (12a–12d смотрят
внутрь через MTProto). Скоуп по шву: ОДНА метрика (внешний взгляд `/channels/stat` на свой канал)
в ОДНОМ месте (секция «🌍 Рынок» на экране «📈 Рост»). 0 AI-вызовов. План:
`.claude/plans/12e-market-telemetr.md`.
- ENV: `TELEMETR_API_KEY` (опционален, как PEXELS) — без него секции просто нет.
- ЯДРО (`core/market/`): `marketData.ts` (тип + интерфейс `MarketDataProvider` — риск №4, ядро не
  знает про Telemetr) · `marketCache.ts` (кэш в `Setting`, ключ `market_stat_cache`, TTL 12ч —
  бережёт лимит 10k/мес) · `marketSection.ts` (плейн-форматтер «🌍 Рынок», сравнение ERR Telemetr
  со своим ERR за 7д из снимка 12b; БЕЗ Markdown-эмфазы — правило 12c).
- АДАПТЕР (`services/market/telemetrProvider.ts`): единственное место, знающее про Telemetr
  (`api.telemetr.me`, Bearer, zod-safeParse); нет ключа/429/ошибка/кривой JSON → null + warn.
- СЕРВИС (`services/market/marketStatService.ts`): свежий кэш → 0 запросов; протух → запрос;
  упал → протухший кэш; приватный канал (без @) → секции нет.
- ВЖИВЛЕНИЕ: `BotDeps`/`AdminDeps` += `telemetrApiKey`; `renderGrowth` — секция ПОСЛЕ
  отчёта/AI-пересказа (в Haiku рынок не скармливаем).
- ⚠️ Прод: положить `TELEMETR_API_KEY` в env Railway (владелец решил НЕ перевыпускать засвеченный
  ключ — «пока устраивает»). Ключ — только в env, НЕ в git.
- ⏭ Дальше — **12e-2** (похожие каналы, динамика подписчиков, бенчмарк ниши, секция в еженедельном
  отчёте) либо следующий шаг роадмапа.

**Шаг 12d ГОТОВ** (typecheck 0, lint 0, vitest **320/320** [+10], build ок, миграций НЕТ):
AI-НАРРАТИВ отчёта «Рост» — опциональный пересказ готовых фактов 12c голосом канала через Haiku.
Никакой новой аналитики: LLM только переформулирует (принцип 11c/11e), фолбэк — сухой текст 12c.
Сделан ЦЕЛИКОМ (экран + отчёт, шов 12d-1/12d-2 не понадобился).
- ЯДРО (`core/ai/buildGrowthNarrativePrompt.ts`): промпт из тона канала + готового текста
  `buildInsightsReport`; system запрещает выдумывать факты/числа; `parseNarrative` (zod, лимит 1500,
  кривой → null) вычищает `*`/`_` (текст идёт и в Markdown-отчёт, и на плейн-экран — правило 12c).
- СЕРВИС (`services/ai/growthNarrativeService.ts`): `generateGrowthNarrative` (калька `generateReply`,
  `CLASSIFY_MODEL`/Haiku, инъекция клиента, мягкая деградация → null) + оркестратор
  `narrateGrowthReport` (тумблер → ключ → тон канала → `tryConsumeDailyBudget` ОБЩИЙ, списание ДО
  вызова → Haiku; любой отказ → текст 12c без изменений, расход 0).
- ХРАНЕНИЕ: `growth_narrative_enabled` в `Setting` (дефолт ВЫКЛ), `growthNarrativeSettings.ts`.
- ВЖИВЛЕНИЕ: экран «📈 Рост» (пересказ + кнопка-тумблер `🧠 AI-пересказ`, callback `gntgl`) +
  секция роста еженедельного отчёта (`WeeklyReportDeps` += `anthropicApiKey?`/`timeoutMs?`,
  прокинуты в планировщике и тест-кнопке `anrep`).
- ⚠️ Прод: пересказ виден при `ANTHROPIC_API_KEY` + ВКЛ тумблер + непустой бюджет; каждый показ
  экрана/отчёта при ВКЛ = 1 вызов Haiku (списывает единицу общего `ai_daily_cap`).
- ⏭ Дальше — **12e** (Telemetr за адаптером `MarketDataProvider`; перевыпустить засвеченный ключ).

**Шаг 12c ГОТОВ** (typecheck 0, lint 0, vitest **310/310** [+10], build ок, миграций НЕТ):
ВЫВОД Content Intelligence — впервые ПОКАЗЫВАЕТ владельцу выводы (еженедельный отчёт + экран «📈 Рост»).
Ядро 12a считает, 12b/12b-2 наполняют — 12c форматирует. 0 токенов, всё из готовых таблиц. Сделан ЦЕЛИКОМ.
- ЯДРО: `dimensions.ts` += `contentDimensionStats`/`lengthBucket` (ERR по медиа/кнопкам/длине);
  `advisor.ts` (`buildAdvice(insights, contentStats, snapshot)` → `Advice[]`, факты+приоритет: слоты,
  нативные часы `matchesOwn`, тренд+Δ подписчиков, контент, выбросы; порог `MIN_POSTS_FOR_ADVICE=3`);
  `insightsReport.ts` (`buildInsightsReport` → текст «зашло/провалилось/время/рекомендации», ⚠️ БЕЗ
  Markdown-эмфазы — один текст и в Markdown-отчёт, и в плейн-экран); `topHours.ts` += `sortTopHours`.
- РЕПОЗИТОРИЙ: `listRecentStatSnapshots` (2 снимка для Δ подписчиков); `getLatestStatSnapshot` — обёртка.
- СЕРВИС: `buildChannelIntelligence` += `contentStats` + `latest/previousSnapshot`; новый
  `buildGrowthReport` (БД-only, нативные часы UTC→пояс канала, → советник → форматтер).
- ВЫВОД: секция в `weeklyReportService` после чисел 7c + экран `renderGrowth` (кнопка `grow` в меню).
- ⚠️ Прод: выводы скудны без данных — их наполняют джоб снимка (22:00 МСК) и отчёт (ПН 09:30 МСК),
  оба требуют MTProto + прав админа канала. Тренд по просмотрам (12a) считается по всему окну с выбросами.
- ⏭ Дальше — **12d** (AI-нарратив: тот же отчёт голосом канала через Haiku, опц. тумблер, фолбэк на 12c)
  либо **12e** (Telemetr за адаптером `MarketDataProvider`).

**Шаг 12b-2 ГОТОВ** (typecheck 0, lint 0, vitest **300/300** [+9], build ок, миграция применена локально):
НАТИВНАЯ СТАТА Telegram — лучшие часы канала из `stats.getBroadcastStats`. Отложенный хвост 12b.
- ЯДРО (`core/analytics/topHours.ts`, +9 тестов): чистый парсер графа Telegram (`DataJSON.data`,
  `{"columns":[["x",0..23],["y0",…]]}`) → `TopHour[]` {hour,value}; `parseTopHoursGraph` + `rankTopHours`
  (best-first). Битый JSON / час вне 0..23 / нечисло → []/пропуск.
- СБОР (`mtprotoClient.ts`): `fetchTopHours` — обработка `STATS_MIGRATE_X` (стата на профильном DC →
  `invoke(req, dcId)`), догрузка `StatsGraphAsync` через `loadAsyncGraph`; любая ошибка → [] (мягко).
- ХРАНЕНИЕ: миграция `..._snapshot_top_hours` — `ChannelStatSnapshot.topHours Json?`. Пишем всегда
  массив (обход Prisma DbNull), читаем мягким `toTopHours`. Джоб снимка (22:00 МСК) кладёт часы в снимок;
  `buildChannelIntelligence` уже отдаёт `latestSnapshot` → 12c получит нативные часы бесплатно.
- ⚠️ Прод: нужны права админа канала у MTProto-аккаунта (иначе часы пустые, снимок всё равно ляжет);
  стата появляется у канала не сразу (нужен минимум охвата).
- ⏭ Дальше — **12c**: отчёт/экран «📈 Рост» (свои слоты + нативные часы, тренд по снимкам, эвристический
  советник; AI-нарратив — 12d).

**Шаг 12b ГОТОВ** (typecheck 0, lint 0, vitest **291/291** [+2], build ок, миграция применена локально):
ДАННЫЕ И СБОР для Content Intelligence — наполняет ядро 12a реальными данными. Второй подшаг эпика 12.
- СХЕМА (миграция `..._step12b_content_dimensions_snapshot`): `PostMetric` += `hasMedia`/`hasButtons`
  (bool, деф. false) + `charLen` (int, деф. 0) — контентные измерения «что заходит»; новая таблица
  `ChannelStatSnapshot` (subscribers?, postCount7d, avgViews7d, avgErr7d Float) — снимки для тренда охвата.
- ЯДРО (`core/analytics/weeklyReport.ts`): `PostMetricInput`/`RawMessageLike` += измерения; `messageToMetric`
  считает медиа/кнопки (`media`/`replyMarkup`→bool) и `charLen` = ПОЛНАЯ длина (не обрезанное превью).
- СБОР (`mtprotoClient.ts`): окно 30→**100** сообщений (тренд = 2 окна по 7д); новая `fetchSubscriberCount`
  (`channels.getFullChannel`→`participantsCount`, ошибка→null). `stats.getBroadcastStats` (лучшие часы)
  СОЗНАТЕЛЬНО вынесена в **12b-2**, чтобы не раздувать подшаг (план разрешил).
- РЕПОЗИТОРИИ: `upsertPostMetric` пишет новые поля; `listPostMetricsSince` (чтение); новый
  `channelStatSnapshotRepository` (`createStatSnapshot` append-only + `getLatestStatSnapshot`).
- СЕРВИС `contentIntelligenceService.ts`: `buildChannelIntelligence` (БД-only, 2 окна по 7д → `Insights`
  ядра 12a + последний снимок; 0 токенов — для 12c) + `runStatSnapshot` (ДЖОБ: MTProto свежий сбор +
  подписчики + агрегаты `periodStat` → снимок; `destroy()` в finally; без MTProto/канала — тихо).
- ДЖОБ (`scheduler/analytics.ts`): снимок охвата ежедневно **22:00 МСК** (3-й крон, `protect`, изоляция
  GramJS через динамич. импорт сохранена).
- ⚠️ Прод: миграцию на Railway применит `prisma migrate deploy` (в `npm start`); снимок пишется только при
  настроенном MTProto, иначе джоб тихо ничего не делает.
- ⏭ Дальше — **12b-2** (нативная стата: лучшие часы Telegram) либо сразу **12c** (отчёт/экран «📈 Рост»
  поверх `buildChannelIntelligence`).

**Шаг 12a ГОТОВ** (typecheck 0, lint 0, vitest **289/289** [+21], build ок): ЯДРО Content Intelligence
— чистая математика превращения сырых метрик в выводы. Первый подшаг эпика 12 «Content Intelligence +
Growth Advisor» (план `.claude/plans/12-content-nifty-river.md`). Ни Telegram/БД/AI — только `core`.
- ЗАЧЕМ: еженедельный отчёт 7c даёт сырые числа без выводов. 12a считает «что зашло / когда публиковать /
  растёт ли охват», задаёт «словарь метрик» для 12b (сбор+сервис) и 12c (отчёт+экран «📈 Рост»). 0 токенов.
- НОВЫЕ МОДУЛИ `src/core/analytics/` (+21 тест, `tests/contentIntelligence.test.ts`):
  `engagement.ts` (ERR=(реакции+комменты)/просмотры, ≤0→0) · `dimensions.ts` (`timeDimensions`→
  hour/weekday/slot, порог `MIDDAY_HOUR=15`, реюз `localDateParts`) · `outliers.ts` (риск №5: `median`+
  `flagViewOutliers`, ≥3× медианы=виральный/рекламный, `MIN_SAMPLE_FOR_OUTLIERS=4`) · `bestTime.ts`
  (`rankPostingTimes` — ср. ERR по день×слот, лучшее первым, выбросы вон) · `trend.ts` (`compareTrend` —
  Δ% просмотров/ERR неделя-к-неделе, up/down/flat, `FLAT_THRESHOLD_PCT=5`, пустая база→null) ·
  `insights.ts` (`buildInsights`→`Insights`{count,best/worst по ERR,bestTimes,trend,outliers}; выбросы
  исключены из лучший/худший и из времени, отданы отдельно). Только факты — текст в 12c, AI в 12d.
- РЕШЕНИЯ (план): время — своя история + нативная стата Telegram (`stats.getBroadcastStats`, 12b);
  тренд — лёгкая таблица `ChannelStatSnapshot` (12b, миграция); вывод И в отчёт, И в экран «📈 Рост»;
  советник сначала эвристики (12c), AI — 12d; Telemetr (12e) за адаптером `MarketDataProvider` +
  мягкая деградация — платный сервис НЕ фундамент (риск №4), свой канал живёт на MTProto бесплатно.
- ⚠️ Telemetr-ключ владельца — ТОЛЬКО в env (не в коде/git); засветился в переписке → перевыпустить перед 12e.
- ⏭ Дальше — **12b**: миграция (поля медиа/кнопки/длина на `PostMetric` + `ChannelStatSnapshot`) +
  обогащение MTProto-сбора + `stats.getBroadcastStats` + сервис `contentIntelligenceService` + джоб снимка.

**Шаг 11f ГОТОВ** (typecheck 0, lint 0, vitest **268/268**, build ок): авто-регистрация AI-триггера
из CTA опубликованного поста — закрывает UX-нюанс 11c (триггеры больше не надо заводить руками).
- ЗАЧЕМ: владелец публикует «напишите СЛОВО в комментах» → бот САМ добавляет слово в `ai_trigger_words`
  → стадия 11c отвечает голосом канала. Без ручной сверки «а добавил ли я триггер». 0 токенов.
- ЯДРО под тестами (`core/triggers/extractTriggerFromCta.ts`, +8 к triggers): `extractTriggerFromCta(cta)`
  — эвристика по приоритету: слово/фраза в кавычках («…»/"…"/„…“) → слово КАПСОМ (≥3 буквы). Валидация
  через `normalizeTriggerText`: длина 3…40, ≤3 слов. Кавычки в приоритете (однозначнее КАПСа). Нет → null.
- ВЖИВЛЕНИЕ (`services/postingService.ts`): хелпер `registerCtaTrigger` = `extractTriggerFromCta` →
  `validateTriggerWord` (дедуп по норме) → `addAiTriggerWord` (11c, идемпотентно). Ошибки глотает
  (не роняет публикацию). Зовётся во ВСЕХ точках выхода в канал: автопостинг, AI-подхват (10c),
  разовый пост (6c), `publishPending` (одобрение). Превью админу триггер НЕ регистрируют.
- Решения владельца: ОТКУДА — эвристика по CTA; КУДА — только AI-триггеры; КОГДА — при публикации;
  снятие устаревшего триггера — НЕ делаем (только добавляем), в `docs/BACKLOG.md`.
- ⚠️ Прод: триггер регистрируется автоматически; чтобы бот ОТВЕТИЛ — как 11c: `ANTHROPIC_API_KEY` +
  ВКЛ AI-ответы + бюджет. Без этого триггер есть, ответа нет (расход 0).
- ⏭ Бэклог (`docs/BACKLOG.md`): сбор идей из обсуждений; снятие устаревшего триггера; проверка
  «админ группы» в модерации; 11c-fix (ответы анонимам). Дальше — по роадмапу (`ПЛАН.md`).

**Шаг 11e ГОТОВ** (typecheck 0, lint 0, vitest **260/260**, build ок): семантическая модерация
ТОКСИЧНОСТИ через Haiku — второй слой `moderationStage` после дешёвых эвристик 11d.
- ЗАЧЕМ: эвристики 11d слепы к смыслу; враждебный коммент без ссылок/стоп-слов («автор —
  шарлатанка») для регэкспов «чистый». 11e даёт AI-слой в хук `borderline`.
- ХРАНЕНИЕ (без миграции, в `Setting`): `moderation_toxicity_enabled` (bool, дефолт **ВЫКЛ**,
  ОТДЕЛЬНЫЙ тумблер) + `moderation_toxicity_policy` (JSON string — своё правило канала, пусто =
  авто по нише). Действие переиспользует `moderation_delete`; бюджет ОБЩИЙ с AI-ответами
  (`ai_daily_cap`/`tryConsumeDailyBudget`).
- ЯДРО под тестами (`core/moderation/buildToxicityPrompt.ts`, +13 к moderation): промпт по
  `niche/toneOfVoice/language` канала (**niche-agnostic по данным**, как `buildReplyPrompt`) +
  опц. `policy`; `TOXICITY_JSON_SCHEMA` (Structured Outputs) + `parseToxicityVerdict`
  (zod `{toxic,reason}`, кривой JSON → null); пред-фильтр `shouldCheckToxicity` (нормализованная
  длина ≥3 — не гоняем Haiku на эмодзи/односимвольные).
- СЕРВИС `services/moderation/toxicityService.ts` `classifyToxicity(deps,input,client?)`: калька
  `aiReplyService`, `CLASSIFY_MODEL` (Haiku) + JSON-схема, обрезка коммента 500, нет ключа/ошибка/
  кривой ответ → null (мягкая деградация), клиент инъектируется в тестах.
- СТАДИЯ: действие спам- и токсичного слоёв вынесено в общий `enforce(ctx,deps,channelId,
  reasonLabel)` (delete-if-enabled + сигнал админу). Токсичный слой (`checkToxicity`) зовётся
  после `detectSpam`=не-спам, ворота дёшево→дорого: привилегия → тумблер → пред-фильтр →
  `getReplyChannelById` → `tryConsumeDailyBudget` (дата в TZ канала) → `classifyToxicity` →
  toxic → `enforce`. Бюджет списываем ДО вызова. `CommentDeps` НЕ менялся (`anthropicApiKey`/
  `timeoutMs` с 11c, `adminId` с 11d).
- МЕНЮ: экран «🛡 Модерация» дополнен тумблером `🧠 Токсичность (AI)` (`toxtgl`) + `📝 Политика`
  (`toxpol`, pending `setToxicityPolicy`, «-»/пусто = сброс на авто). Заметка «тратит токены,
  лимит общий с AI-ответами».
- Решения владельца: охват — враждебность к каналу/аудитории, но per-channel по нише (SaaS);
  настройка — авто + своё поле политики; действие — отдельный тумблер, дефолт сигнал; бюджет общий.
- ⚠️ Прод (как 11c/11d): `ANTHROPIC_API_KEY` + ВКЛ модерацию + ВКЛ токсичность → враждебный
  коммент без ссылок/стоп-слов от не-админа → сигнал админу; своя политика влияет; бюджет 0 → не зовём.
- ⏭ Engagement Engine (Модуль 5): модерация закрыта (эвристика 11d + AI-токсичность 11e). Дальше —
  по роадмапу (`ПЛАН.md`); кандидат **11f**: авто-связка «слово в CTA поста ↔ AI-триггер».

**Шаг 11d ГОТОВ** (typecheck 0, lint 0, vitest **247/247**, build ок): эвристическая модерация
комментов БЕЗ AI и БЕЗ токенов — `moderationStage` (была заглушка) теперь первой в конвейере.
- ХРАНЕНИЕ (без миграции, всё в `Setting`): `moderation_enabled` (bool, дефолт **ВЫКЛ**) +
  `moderation_delete` (bool, дефолт **ВЫКЛ** — авто-удаление, нужны права бота) +
  `moderation_stopwords` (JSON `string[]`). Сервис `services/moderation/moderationSettings.ts`
  по образцу `aiReplySettings`.
- ЯДРО под тестами (`core/moderation/detectSpam.ts`, +15): чистая функция
  `{ text, isPrivileged, stopWords? } → { spam, reason? }`. Эвристики (приоритет):
  `link` (URL/`t.me`/голый домен) · `mentions` (≥3 @-упоминаний) · `repeat` (серия ≥4
  одинаковых символов по СЫРОМУ тексту — нормализация ядра их схлопывает) · `stopword`
  (через `containsTrigger`, нормализация обеих сторон). Категория `borderline` — хук под **11e**.
- СТАДИЯ: gate `moderation_enabled` + не привилегированный (`is_bot` аноним/канал ИЛИ владелец)
  + `detectSpam` → действие. Дефолт — **сигнал админу** (`sendMessage(adminId)`, Markdown-fallback,
  ссылка `t.me/c/…`, прав не требует); при `moderation_delete` — `ctx.deleteMessage()` в try/catch
  на `GrammyError` (нет прав → лог + сигнал, мягкая деградация). Возврат: `handled` только при
  удалении, иначе `pass` (нормальные комменты идут к триггеру/AI). `CommentDeps` += `adminId`.
- МЕНЮ: экран «🛡 Модерация» (`mod`) из «⚙️ Настройки»: тумблер + тумблер авто-удаления +
  список стоп-слов (добавить/удалить, пагинация). Паттерн `encodeCb`/pending, валидация
  `validateTriggerWord`.
- Решения владельца: действие — настройка (сигнал по умолчанию, авто-удаление тумблером); модерация
  первой — отсекает мусор ДО того, как AI-стадия потратит токены на ответ спамеру.
- ⚠️ Прод (по регламенту, как 11c): ВКЛ тумблер → коммент со ссылкой от не-админа → сигнал админу;
  ВКЛ «удалять спам» при правах → удаление; без прав → только сигнал.
- ⏭ Дальше — **11e** (токсичность через Haiku, встраивается в хук `borderline`).

**Шаг 11c ГОТОВ и ПРОВЕРЕН В ПРОДЕ** (typecheck 0, lint 0, vitest **232/232**): AI-ответ по
триггер-словам в комментах голосом канала — видимая фича Engagement Engine на ограждениях 11b.
- ХРАНЕНИЕ (без миграции, всё в `Setting`): `ai_reply_enabled` (boolean, дефолт **ВЫКЛ**) +
  `ai_trigger_words` (JSON `string[]`, ОТДЕЛЬНЫЙ набор — пул готовых текстов не трогаем).
  Дневной лимит — существующий `ai_daily_cap` (11b) + сеттер `setDailyCap` (пер-канальный, дефолт 50).
- ЯДРО под тестами: `core/triggers/containsTrigger.ts` («сообщение СОДЕРЖИТ слово», границы по
  пробелам, `кот ⊄ который`) · `core/ai/buildReplyPrompt.ts` (билдер тон-канала→промпт +
  zod `parseReplyText`, лимит 600, null при пусто/длинно). Сервис `services/ai/aiReplyService.ts`
  на дешёвой `CLASSIFY_MODEL` (Haiku), `jsonSchema:null`, обрезка коммента, мягкая деградация.
- СТАДИЯ `aiReplyStage` вживлена (была заглушка): ворота от дешёвых к дорогим — не бот →
  канал → тумблер → `containsTrigger` → пер-юзер кулдаун `__ai_reply` (час из общей настройки) →
  `tryConsumeDailyBudget` (дата дня в TZ канала) → `generateReply` → ответ в тред. Бюджет
  списываем ДО вызова, кулдаун — только при ответе. Роутинг коммент→канал вынесен в общий
  `comments/routing.ts` (обе стадии). `CommentDeps` += `anthropicApiKey?`/`timeoutMs?`.
- МЕНЮ: экран «🤖 AI-ответы в комментах» (`eng`) из «⚙️ Настройки»: тумблер + дневной лимит +
  список AI-триггеров (добавить/удалить, пагинация). Паттерн `encodeCb`/pending.
- Решения владельца: AI-кулдаун ОБЩИЙ с триггерами; `ai_daily_cap` пер-канальный (SaaS).
- ⚠️ Прод: нужен `ANTHROPIC_API_KEY` на Railway + ВКЛ тумблер + непустой набор. Иначе молчит (расход 0).
- ✅ ПРОВЕРЕНО В ПРОДЕ (@sofia_gada1ka): ключ на Railway есть (кнопка «🤖 AI-пост» генерит), тумблер
  ВКЛ, лимит 50, триггеры заданы. AI отвечает РЕАЛЬНЫМ юзерам. ДИАГНОЗ «не отвечает при тесте»:
  владелец писал комменты ОТ ИМЕНИ КАНАЛА (анонимно) → Telegram шлёт `GroupAnonymousBot`
  (`from.is_bot=true`), а `aiReplyStage` такие НАМЕРЕННО пропускает (`is_bot` → `pass`); пул же
  анонимов обрабатывает («Лови послание»), потому пул отвечал, AI молчал. Плюс `containsTrigger` =
  границы слова (склонения «домой/дома» НЕ ловит; «кофе» не склоняется — совпадает).
- ⚠️ НЮАНС UX: AI-триггеры вводятся ВРУЧНУЮ; генератор AI-поста НЕ регистрирует призывное слово из
  CTA автоматически. Кандидат на будущее (**11f**): авто-связка «слово в CTA поста ↔ AI-триггер».
- ⏭ Дальше — **11d** (эвристический антиспам, 0 токенов: сигнал админу + тумблер `moderation_delete`).
  Опц. хвост **11c-fix**: отвечать анонимам, но игнорировать `is_automatic_forward` (автопересылку).

**Шаг 11b ГОТОВ** (typecheck 0, lint 0, vitest **214/214**): защита от расхода токенов =
фундамент Engagement Engine (Модуль 5). Видимой фичи нет; генерация постов не изменилась.
- РОУТИНГ МОДЕЛЕЙ: `CLASSIFY_MODEL="claude-haiku-4-5"` рядом с `GENERATION_MODEL` (Opus) —
  дёшево для ответов/классификации (11c/11e), дорого только для генерации постов.
- ТАЙМАУТ на Claude (просьба владельца): `createAnthropicClient(apiKey, {model,maxTokens,
  timeoutMs,jsonSchema})`, дефолт `DEFAULT_AI_TIMEOUT_MS=15000`, передан 2-м аргументом SDK;
  env `AI_TIMEOUT_MS` (опц.) протянут через deps (`timeoutMs?` везде, мягко).
- ДНЕВНОЙ БЮДЖЕТ: чистое ядро `core/ai/dailyBudget.ts` (тесты, +6 ожидается) + сервис
  `services/ai/aiBudget.ts` `tryConsumeDailyBudget` поверх `Setting` (`ai_budget_usage`/
  `ai_daily_cap`, дефолт 50). Пер-юзер лимит 11c возьмёт из готового `Cooldown` (`__ai_reply`).
- Миграций/зависимостей нет. Решения владельца: AI-ответы по триггер-словам (отдельный набор,
  пул не трогаем), модерация — эвристический антиспам на старте, «сначала защита от расхода».
- ⏭ Дальше: 11c (AI-ответ по триггерам) → 11d (антиспам). План: `.claude/plans/11-engagement-*`.

**Шаг 11a ГОТОВ** (код, typecheck/lint зелёные, vitest **208/208**): календарь контент-плана
+ фикс «план застрял на Неделе 1».
- КОРНЕВАЯ ПРИЧИНА повтора найдена: `Channel.campaignStart` никогда не записывался (в сидже
  `null`), а `resolveCampaignDay` при `null` всегда даёт неделю 1 → план стоял на месте. Фикс —
  `ensureCampaignStart` (идемпотентный якорь): вызывается в `publishDuePostsForChannel` (первый
  активный тик = самоисцеление `@sofia_gada1ka`) и в `atgl` (включение автопостинга). Схема не
  менялась, миграции нет.
- НОВЫЙ ЭКРАН «📅 Календарь» (широкая кнопка в главном меню, callback `cal`): «Неделя N из 4»,
  сегодняшний день, посты недели с маркерами ✅ прошёл / ▶️ сегодня / 🔜 впереди; тап по посту
  → тот же редактор (`pp`). Чистый хелпер `core/schedule/postStatus.ts` под тестами (+6).
- ⏳ За пользователем — ручная проверка в TG (см. конец записи 11a в журнале). На проде первый
  тик автопостинга проставит `campaignStart`, со следующей недели план пойдёт на Неделю 2.
- ⏭ 11b (если после 4 недель повтор мешает): AI-вариации/ротация на 28-дневном лупе.

**Шаг 10 ЗАКРЫТ** (память канала 10a + AI-пост 10b + самообновление 10c).
- 10b введён в прод и проверен (`ANTHROPIC_API_KEY` на Railway, миграция применилась, кнопка
  «🤖 AI-пост» вернула превью голосом канала; «🔄 Другое фото»/«✍️ Изменить текст» работают).
  MTProto-сессия — владелец подтвердил, сделана.
- 10c (код готов, typecheck/lint/test зелёные, 202/202): автопостинг сам подхватывает
  AI-генерацию, когда на слот нет готового поста — тумблер «🤖 AI-подхват» в экране автопостинга;
  публикация/одобрение AI-поста подчиняются существующему тумблеру «📋 Одобрение» (ВКЛ → превью
  админу, ВЫКЛ → сразу в канал). + кнопка «👀 Предпросмотр» на превью — показывает пост как в
  канале (реальное фото + настоящие кнопки поста). Детали — в `docs/ARCHIVE-PROGRESS.md`.

**Дальше — Шаг 11** (Engagement Engine: AI-ответы в комментах в тоне канала, детект токсичности,
фильтр спама). Отложено в Шаге 10: Media Engine (AI-генерация картинок) + роутинг моделей Claude;
ввод темы поста в мастер «🤖 AI-пост» (поле `topic` в `buildPostPrompt` готово).
⏳ За пользователем — ручная проверка 10c на `@supertestmaster` (см. конец записи 10c в журнале).

Шаг 10b (продукт поверх фундамента 10a): админ жмёт «🤖 AI-пост» → бот пишет пост голосом
канала → кладёт его в ту же очередь одобрения, что и плановый пост, так что «✅ Опубликовать» /
«✍️ Изменить текст» / «🔄 Другое фото» работают без правок. Что сделано:
- миграция `PendingPost.pexelsQuery` (`20260704073430_...`) + проведена через `pendingPostRepository`;
- `postingService`: `ApprovalDraft` + `requestApprovalForDraft` — единый путь очереди (плановый
  и AI); `requestApproval(post)` стал тонкой обёрткой (плановое поведение прежнее);
- `services/ai/aiPostApprovalService` (`requestAiPostApproval`): ключ→канал→`getSamplePosts`→
  `generatePostDraft`→очередь; результат-union `{no_key|no_channel|no_samples|gen_failed}` →
  понятный тост, не исключение;
- починка «🔄 Другое фото» для AI-постов (`handleReroll`: fallback на `pending.pexelsQuery`);
- кнопка «🤖 AI-пост» (широкая строка `MAIN_SECTIONS`, callback `aigen`) + хендлер;
- проброс `ANTHROPIC_API_KEY`: `index.ts` → `BotDeps` → `AdminDeps`.
⚠️ Прод-нюанс: кнопка сгенерирует пост только с `ANTHROPIC_API_KEY` в env Railway (без ключа —
сообщение-подсказка, без падения). Миграцию `pexelsQuery` на Railway применит `prisma migrate
deploy` (в `npm start`) сам.
⚠️ Прод-шаги MTProto-хотфикса всё ещё за пользователем: `npm run gen-session` → обновить
`TELEGRAM_SESSION` на Railway → redeploy → `/start` боту. Не гонять одну сессию с двух IP.
Дальше — Шаг 11 (AI-ответы в комментах) либо доводка 10 (ввод темы поста в мастер AI).

Отложено: шифрование bot-токенов клиентов + мультибот-форк (остаток Шага 9 — нужно поле
`Channel.botToken` и раннер на токен; имеет смысл только в настоящем мультитенанте, отдельный план);
мультиканальная аналитика (еженедельный отчёт). Пер-постовые ответные комменты не делаем —
триггеры остаются ГЛОБАЛЬНОЙ настройкой канала (`Channel.triggerWords` + `comments_enabled`).

## ✅ Сделано (Фазы A, B и старт C)
Шаг 0 (каркас) · 1a (БД+схема) · 1b (сид «канал №1») · 2 (триггеры+кулдаун) ·
3 (меню админа: CRUD триггеров) · 4 (автопостинг) · 5 (одобрение постов) ·
6a (фото dual-provider) · 6b (кнопки на постах) · 6.5 (ручной редактор контент-плана) ·
доработка 6b (редактор пулов button_*) · 7a (напоминалка о конце контента) ·
7b (инфра MTProto+GramJS) · 7c (еженедельный отчёт + PostMetric) ·
7d (новый контент-план + 11 пулов + runbook смены бота — ФИНАЛ ФАЗЫ B) ·
8a (реестр каналов + переключатель) · 8b (мультиканальный автопостинг) ·
8c (маршрутизация триггеров в комментах по группе обсуждения) ·
9a (онбординг канала: авто-регистрация по `my_chat_member` + проверка прав бота) ·
доработка UI (настраиваемый кулдаун + удаление тестовой публикации) ·
6c (разовый пост в расписание: oneOff в `Post` + мастер «Новый пост» + публикация по `publishAt`) ·
хотфикс MTProto/устойчивости (destroy + сообщение о мёртвой сессии + bot.catch + setMyCommands) ·
аудит надёжности (protect в cron + auto-retry + прогресс по-времени + онбординг от владельца) ·
UX админ-меню (группировка 2×5 + свежие тексты + pluralRu) ·
10a (фундамент AI-генерации: `core/ai` + `aiGenerationService` + `tryGeneratePost`) ·
10b (AI-пост → очередь одобрения: миграция `PendingPost.pexelsQuery` + `ApprovalDraft`/
`requestApprovalForDraft` + `requestAiPostApproval` + кнопка «🤖 AI-пост» + фикс reroll).

Тесты сейчас: **vitest 429/429**, tsc 0, eslint 0.

## 📌 Ключевые решения
- Стек: TS strict, grammY, zod, pino, vitest, ESLint (no-any).
- **Prisma 7.8 + PostgreSQL.** Локально PG 18 (`aicm_dev`, user `postgres`/`postgres`).
  Прод — облачный Postgres, переключение через `DATABASE_URL`.
- **Prisma 7:** `url` НЕ в schema.prisma, а в `prisma.config.ts`; рантайм — адаптер
  `@prisma/adapter-pg`. Генератор → `src/generated/prisma` (в .gitignore). `.env` через
  `process.loadEnvFile()` (без dotenv).
- Хостинг: **Railway** (~$5/мес, long polling). Репо:
  https://github.com/vovabakin96416-sketch/MasterPost (публичный).
- Секреты только в `.env` (gitignore) и env хостинга — в git НЕ коммитим.
- **ADMIN_ID** в env (zod, обязателен). Локально `7035079048`.
- Цель публикации задаётся НЕ в коде, а из `/menu → Автопостинг → Указать канал`.
  Прод-канал `@sofia_gada1ka`, тест-канал `@supertestmaster`.

## ⚠️ Не трогать
- Python-бот (референс): `Soffia\04-Бот\bot\bot.py`
- Папка `taro30`

## ⏳ За пользователем (ручные проверки в Telegram)
Полный список ручных проверок по каждому шагу — в конце соответствующей записи
`docs/ARCHIVE-PROGRESS.md`. Прод-нюансы шага (миграции/env/сид) — там же помечены ⚠️.
