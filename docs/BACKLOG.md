# BACKLOG — отложенное (владелец счёл лишним пока)

Короткий список задач, сознательно отложенных. Одна строка = одна задача.
Брать в работу только по явному запросу владельца.

- **Сбор идей для постов из обсуждений** — последняя часть Модуля 5 (Engagement Engine). Отложено.
- **Снятие AI-триггера при устаревании поста** — 11f только ДОБАВЛЯет слово-триггер (идемпотентно); авто-удаление «отжившего» триггера не делаем.
- **Проверка «админ группы» в модерации** — 11d/11e считают привилегированным владельца/анонима канала; отдельная проверка админов группы обсуждения — потом.
- **11c-fix (ответы анонимам)** — `aiReplyStage` намеренно пропускает `is_bot` (`GroupAnonymousBot`); при желании отвечать анонимам — игнорировать `is_automatic_forward` (автопересылку).
- **SaaS-онбординг: профиль голоса** (Шаг 14+) — анкета голоса канала (примеры, запрещённые слова, обороты) для новых клиентов без истории; для больших каналов — извлечение из их же постов. Против «дефолтного голоса Claude» у всех клиентов.
- **SaaS-онбординг: сид не тиражировать** (Шаг 14+) — `seed/data/content.json` — контент канала №1; новым клиентам общий контент только как шаблоны с AI-переписыванием в их голос, иначе каналы одной ниши публикуют одинаковые посты.
- **13f — AI-советник экспериментов** — Haiku по инсайтам 12c предлагает «что тестировать следующим», владелец подтверждает кнопкой (см. `.claude/plans/13-experiments.md`).
