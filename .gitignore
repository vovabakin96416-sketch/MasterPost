# Зависимости и сборка
node_modules/
dist/
coverage/

# Сгенерированный Prisma-клиент (build-артефакт, создаётся `prisma generate`)
src/generated/

# Секреты — НИКОГДА не коммитим (репозиторий публичный)
.env
.env.local
.env.*.local

# Логи
*.log

# Системное
.DS_Store
Thumbs.db

/src/generated/prisma

# Одноразовые проверочные скрипты сессий (verify*.tmp.ts) — вне git
scripts/*.tmp.ts
